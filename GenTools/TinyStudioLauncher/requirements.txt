# TinyStudioLauncher requirements
# Core dependencies for the launcher application

# UV for fast package management
uv>=0.5.14

# Qt framework for UI (PySide6 preferred, PySide2 for compatibility)
PySide6>=6.5.0; python_version >= "3.8"
PySide2>=5.15.2; python_version < "3.8"

# Process and system utilities
psutil>=5.9.0

# File watching for environment changes
watchdog>=3.0.0

# Configuration management
pydantic>=2.0.0

# Logging enhancements
colorlog>=6.7.0

# Development dependencies
pytest>=7.4.0
pytest-qt>=4.2.0
black>=23.0.0
mypy>=1.0.0 