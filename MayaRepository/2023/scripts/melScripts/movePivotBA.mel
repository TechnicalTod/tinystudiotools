global proc movePivotBA(){
    string $selection[] = ls("-sl");
    float $pivot[];
    for ($each in $selection) {
    if (size($pivot) == 0) {
    $pivot = xform("-q","-ws","-rp",$each);
    }
    xform("-ws","-piv",$pivot[0],$pivot[1],$pivot[2],$each);
    }
}