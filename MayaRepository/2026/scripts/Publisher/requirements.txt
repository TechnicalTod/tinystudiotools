# Core dependencies
PySide6>=6.2.0
sqlite3

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0

# Optional dependencies (for advanced features)
# Maya Python API (installed with Maya)
# - maya-python-api (this comes with Maya installation) 